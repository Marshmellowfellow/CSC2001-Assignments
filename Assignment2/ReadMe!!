

Run make to produce class files.

Run the bash script(./bashscript)  within bin to repoduce the tests from the report in the results directory..

Run either: java PowerAVLApp or java PowerBSTApp without any arguments to print all the dates and information. Or optionally run them with any of  additional arguments.

|Arguments:
|    -s <search query>, search for the power information for a particular time stamp.
|    -k <keyfile>, search a file of keys for there respective time stamp information.
|    -l <size>, adjust the size of the subset of data being searched through.
|    -c <optional filename>, display number of operations completed for that search, with optional writing output to a file.

|For the PowerAVLApp including the -u argument indicates if the tree is too be built with a sorted array.    
